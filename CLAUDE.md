# カフェイン摂取管理アプリ 要件定義

## 概要
日々のカフェイン摂取を簡単に記録し、今日の総摂取量と体内への推定残存量を確認できるアプリ。

## 機能要件

### 1. カフェイン摂取の記録
- 飲料・摂取量・摂取時刻を登録できる
  - 摂取時刻はデフォルトで現在時刻が入力され、必要に応じて変更できる
- コーヒー・お茶・エナジードリンクなどをプリセットから選択できる
- プリセットを使わず手動入力もできる
- プリセットを選択すると、その飲料に応じた摂取量（カフェイン量）が自動入力される
- よく使う記録はワンクリックで入力を完了できる（クイック追加）

### 2. 摂取状況の確認
- 今日の総摂取量を確認できる
- 現在の体内への推定残存量を確認できる
- 今日どの飲み物を何杯（何回）飲んだかの内訳を確認できる

### 3. 履歴管理
- 過去の摂取履歴を確認できる
  - 履歴一覧の表示範囲は直近1週間とする
  - データ自体はローカルストレージに全期間保持し、1週間より前のデータを自動削除することはしない（表示のみ直近1週間に絞る）
- 飲み物ごとに履歴をフィルターできる
- 履歴を削除できる

### 4. 体内残量の可視化・見通し
- 体内カフェイン残存量の時間推移をカーブ（グラフ）で表示できる（過去の摂取による立ち上がりと、半減期モデルによる減衰を反映）
- 就寝時刻を設定でき、就寝時刻時点の推定残存量を確認できる
- 1日の上限値・就寝時刻から逆算した「これが最後の1杯の目安」となる時刻を確認できる

### 5. 統計
- 摂取傾向を振り返るための統計画面を用意する（表示内容の詳細は今後の設計で詰める）

### 6. 設定
- 1日の摂取上限値を設定・編集できる
- 就寝時刻を設定できる
- 半減期モデルの値（固定5時間）を確認できる（読み取り専用、変更不可）

## ユーザー体験フロー
```
飲み物を登録
    ↓
今日の摂取量が更新
    ↓
現在の体内の推定カフェイン残量が更新
    ↓
時間経過による減少を確認
    ↓
履歴から過去の摂取を確認
```

## 技術要件
- フレームワーク: Next.js（App Router）
- 言語: TypeScript
- データ保存先: ブラウザのローカルストレージ（サーバー・DBは使用しない）
- デプロイ先: Vercel

## 詳細仕様（確認済み）

### 体内カフェイン残存量の計算
- 半減期モデルを使用し、半減期は固定値 **5時間** とする
- 個人差（体重・体質など）による半減期の可変設定は行わない

### データモデル・ロジック層の規約
- カフェイン量はmg、摂取日時はUTCのISO 8601文字列（ミリ秒付き）で保存し、日付の集計・表示・入力は端末のローカルタイムゾーンを使う
- 今日の合計・内訳は現在時刻までの摂取を対象とし、残存量は指定時刻以前の全期間の記録を対象とする
- 飲み物別内訳はプリセットIDでまとめ、手動入力は前後の空白を除いた飲料名でまとめる
- 残量カーブは既定5分間隔でサンプリングし、終端と摂取イベントの直前・直後も含める
- 設定の初期値は上限400mg/日、就寝時刻23:00。就寝時刻は今日のローカル時刻として扱い、既に過ぎていても翌日に繰り越さない
- 「最後の1杯」の逆算では、追加1杯のカフェイン量と就寝時の残量目標を引数で指定する。現行UIでは仮設定として追加1杯 **90mg**、就寝時残量目標 **50mg** を使用する。これらは医学的な安全閾値・安全保証ではなく、計算表示用の固定条件として扱う
  - 当日分の既存記録（未来時刻の記録も含む）と追加1杯の合計が日上限以下であることを確認する
  - 全期間の既存記録による就寝時残量を目標から差し引き、残りの量に収まる追加1杯の最終時刻を半減期モデルで逆算する
  - 今日の現在時刻から就寝時刻までに条件を満たす時刻がない場合は `null` を返す
- localStorageはバージョン付きのJSONで全期間を保存する。未保存時のみ初期値を返し、破損・未知バージョン・利用不可・保存失敗は明示的なエラー結果を返す。読み込み失敗時にデータを自動削除・上書きしない

### 摂取量の警告機能
- 1日の摂取量が推奨上限（デフォルト 400mg/日）に近づいた場合に警告表示を行う
- 上限値はユーザーが設定変更できる

### プリセット飲料
- 代表的な飲料を10種類前後用意する（例: ドリップコーヒー、エスプレッソ、インスタントコーヒー、缶コーヒー、デカフェコーヒー、紅茶、緑茶、烏龍茶、コーラ、エナジードリンクなど）
- 各プリセットにはカフェイン量の目安値をあらかじめ設定しておく
- ユーザーによるプリセットの追加・編集機能は設けない（初期バージョンでは対象外）

### PWA対応
- ホーム画面に追加してオフラインでも使えるようにする
- manifest.json とサービスワーカーによるアプリシェルのキャッシュ対応を行う

### 画面構成
- 「今日 / 履歴 / 統計 / 設定」の4タブ構成とする

## 補足・今後の検討事項
- デザインテイスト（配色・トンマナ）の詳細は未確定
- データのエクスポート・インポート、バックアップ機能は現時点では対象外
- 複数端末間のデータ同期は行わない（ローカルストレージのみのため端末ごとに独立）
