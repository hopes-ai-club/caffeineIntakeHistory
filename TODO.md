# 実行計画（TODOリスト）

`CLAUDE.md` の要件定義に基づく実装計画。フェーズ順に上から着手する想定。
`CLAUDE.md` / `TODO.md` はフェーズ0着手前に初回コミット済みとする。以降は各フェーズごとに担当者がコーディングし、完了したらPUSHする。

## デザイン参照
UI実装は `design/caffeine-log/` のデザインハンドオフを参照して行う。
- `design/caffeine-log/Caffeine Log.dc.html` — 4画面（ホーム／記録追加／体内残量詳細／履歴）のHigh-fidelityデザイン本体（ブラウザで直接開ける）
- `design/caffeine-log/README.md` — 画面仕様・配色/タイポ/余白などのデザイントークン・状態設計・インタラクション仕様
- `design/caffeine-log/ios-frame.jsx` — 端末フレーム（実装不要、プレビュー用）

HTML/インラインスタイルはそのまま流用せず、Next.js（App Router / TypeScript / Tailwind CSS）のプロジェクト規約に沿って **Tailwindクラスで再現** する。配色・タイポ・余白・角丸はREADME記載の値をピクセル単位で再現する（High-fidelity）。

デザイン側で「未設計・要相談」とされている以下の項目は、実装時に都度仕様を補って進める（README末尾参照）:
1. 今日の飲み物別内訳の具体的な見せ方
2. 履歴の飲み物フィルターUIの具体的な見せ方
3. 上限接近／超過時の警告表示の文言・バナー
4. 設定画面の詳細（上限値編集・就寝時刻など）
5. PWAアイコン
6. 統計画面の表示内容（`CLAUDE.md` に機能追加済みだが詳細仕様は未設計）

デザインに含まれていた「就寝時刻からの最後の1杯カットオフ表示」「残量カーブ（減衰グラフ）」「統計タブ」は `CLAUDE.md` の正式な機能要件として追加済みのため、フェーズ1〜3で対応必須の項目として扱う。

## フェーズ0: プロジェクトセットアップ
- [x] Next.js（App Router / TypeScript / Tailwind CSS）プロジェクトを作成

## フェーズ1: データモデル・ロジック層
- [x] 型定義（`Preset` / `IntakeRecord` / `Settings`）
- [x] プリセット飲料データ（代表的な10種類前後、カフェイン量目安値付き）
- [x] 体内カフェイン残存量の計算ロジック（半減期5時間の指数減衰モデル）
- [x] 今日の合計摂取量の計算ロジック
- [x] 今日の飲み物別内訳（種類ごとの回数・合計量）の集計ロジック
- [x] 体内残量カーブのサンプリングロジック（時間軸上の残存量推移を一定間隔で算出）
- [x] 就寝時刻時点の推定残存量、および上限値・就寝時刻から逆算した「最後の1杯」目安時刻の計算ロジック
- [x] localStorageの読み書きユーティリティ
- [x] 日時フォーマット用ユーティリティ（表示用ラベル、`datetime-local`変換など）

## フェーズ2: カスタムフック
- [x] `useIntakeRecords`：記録の追加・削除・localStorageへの永続化
- [x] `useSettings`：1日の摂取上限値・就寝時刻の管理・永続化
- [x] `useNow`：残存量のライブ更新用タイマー
- [x] よく使う記録（クイック追加候補）の算出ロジック

## フェーズ3: UIコンポーネント（デザイン準拠）

### 3-0. デザイン基盤
- [x] Tailwindにデザイントークンを反映（`design/caffeine-log/README.md`のカラー・余白・角丸表）
- [x] フォント設定（IBM Plex Sans / IBM Plex Mono を `next/font/google` で読み込み。数値・時刻・mgは必ずMono）
- [x] ダークテーマ単一構成（`bg.canvas #0E0C0A` / `bg.base #17130F` 等）

### 3-1. ホーム画面（`app/page.tsx`）

基盤実装は完了。以下のチェックは記載した実装範囲の完了を示し、部品接続・仕様待ち・統合検証は未完了項目として管理する。

- [x] ホーム画面の基盤実装（共通トークン、レスポンシブ配置、既存フックとの接続、読み込み・エラー・空状態）
- [x] ヘッダー（「今日」＋日付）
- [x] 総摂取量カード（mono数値、0〜100%のプログレスバー、上限までの残りmg、杯数、上限到達・超過表示。上限接近の警告は下記の残作業）
- [ ] 体内残量カード（mono数値、残量カーブSVG表示）
- [x] カットオフ・バナー（就寝時刻から逆算した「最後の1杯」目安時刻の表示。仮設定：追加90mg・就寝時残量目標50mg）
- [x] きょうの記録リスト（今日の現在までを新しい順に最大3件表示、「すべて」のLinkを配置。遷移先の動作確認は下記の残作業）
- [ ] クイック追加バーの完成（上位2候補のチップによる追加・保存結果の通知は実装済み。追加FABは表示のみで、B担当IntakeFormへの接続待ち）
- [x] タブバー（今日 / 履歴 / 統計 / 設定 の4タブ、指定パスへのLink、現在位置の視覚表示・aria-current。遷移先の動作確認は下記の残作業）

3-1 残作業:

- [ ] 上限接近の判定値・警告文言のチーム合意、仕様担当による `CLAUDE.md` 反映、および警告の実装・検証
- [ ] C担当の残量カード・共通グラフ・詳細の公開Props確認と接続、ホーム側の詳細開閉の実装
- [ ] B担当IntakeFormの公開Props確認とFABからの開閉・保存結果の接続、操作検証
- [ ] 「すべて」・4タブの遷移確認（`/`・`/history`・`/stats`・`/settings`。履歴・統計・設定ページの提供待ち）
- [ ] 全部品統合後の実データ検証（過去分を含む残存量、追加・時計更新による残量とグラフの同期、残量詳細）
- [ ] 全部品統合後の375px・402px・1280pxでのデザイン比較、重なり・横スクロール・コンテンツの隠れ・コンソールエラーの確認、およびbuild・型チェック・lint・テスト・差分チェック

3-1 実装・検証メモ（2026-09-17）:

- 総摂取量の実データ集計、杯数、残り、到達・超過表示、0〜100%の進捗は実装・ブラウザ検証済み。上限接近の判定値・文言はチーム合意と仕様担当による `CLAUDE.md` 反映待ちのため未完了。
- 今日の現在までの記録を新しい順に最大3件表示し、昨日・未来の除外を検証済み。「すべて」は `/history` に接続済みだが、履歴ページが未実装のため遷移確認を残す。
- 上位2候補のクイック追加、操作時刻のUTC ISO保存、保存成功時のみ通知・即時反映、再読み込み後の保持を検証済み。FABは表示のみ（無効）。B担当の `IntakeForm` 本体・公開Props（開閉、保存結果の扱い）の確認後に接続する。
- C担当のホーム用残量カード・詳細・共通SVGグラフは未提供。全期間の記録・設定・共通の現在時刻を渡す公開Propsと詳細開閉の方式を確認して接続する。独自の代替カード・グラフは作成していない。
- 4タブのLink（`/`・`/history`・`/stats`・`/settings`）と現在タブの `aria-current="page"` は検証済み。履歴・統計・設定ページの提供待ちで遷移確認は未検証。接続先はこの3パスを各ページ担当と共有・確認する。
- `npm run build`、`npx tsc --noEmit`、`npm run lint`、`npm test`（2タイムゾーンで各11件）、`git diff --check` 成功。専用ポートの架空データで空状態、追加、保存失敗、読み込み失敗・破損データ保持を検証。通常の保存データは使用していない。
- 実装済み部分は375px・402px・1280pxで横スクロール・重なり・タブによる隠れなし。1280pxで420px中央寄せを確認。検証中にコンソールエラー・ハイドレーション警告なし。残量・グラフの時計更新、上限接近、残量詳細、FABからの入力、遷移先ページおよび全要素統合後のデザイン比較は未検証。

### 3-2. 記録追加（ボトムシート）（`components/IntakeForm.tsx`）
- [x] ボトムシートUI（グラバー・ヘッダー・閉じる導線。背景タップ・「閉じる」・Escで閉じる。出入りは240ms transitionで実装、サイズ選択は要件外のため未実装）
- [x] プリセットグリッド（`CLAUDE.md`記載の10種、選択時にカフェイン量を自動入力）
- [x] 手動入力行（プリセット選択で自動入力、手動編集でプリセット選択解除。手動入力の飲料名は集計仕様に合わせ固定名「手動入力」を使用）
- [x] 摂取時刻入力（デフォルト＝現在時刻、`datetime-local`で変更可能。「いま」チップで現在時刻に戻せる）
- [x] プレビュー行（この1杯を加えた場合の今日の合計、上限超過見込みの強調表示）
- [x] 主ボタン（記録する。保存失敗時は入力を保持したままエラー表示、シートは閉じない）

3-2 実装・検証メモ（2026-09-18）:

- 公開Propsは `{ open: boolean; onClose: () => void }`。開くとき描画中に下書き（選択プリセット・入力量・時刻・エラー）をリセットし、保存成功時のみ `onClose` を呼ぶ。既存の `useIntakeRecords` / `useSettings` / `useNow` をそのまま利用し、新しい状態管理は追加していない。
- 一時的なプレビュー用ルート（`app/dev-intake-form-preview`、コミット対象外）でブラウザ検証: プリセット選択→自動入力、手動編集→プリセット解除、上限超過時のプレビュー赤字化、「いま」チップでの時刻リセット、保存成功時の永続化・クローズ、保存前データ破損時のエラーバナー表示（プリセット・主ボタン無効化）、Escape・背景タップでのクローズを確認。検証後にプレビュー用ルートは削除済み。
- `npx tsc --noEmit` / `npm run lint` 成功。
- 未検証・残作業: A担当によるホームFABからの開閉接続、375px/402px/1280pxでの最終デザイン比較、`npm test`・`git diff --check`は本変更がロジック層に影響しないため未実行。
- 保存失敗時の挙動について: `useIntakeRecords`（フェーズ2実装）は書き込み失敗時にフック全体のstatusを`"error"`にするため、IntakeFormでの保存失敗は入力値を保持しつつも、以降の操作（プリセット選択・提出）が読み込みエラー時と同じバナー表示で無効化される。実際に保存が失敗する状況（ストレージ容量超過・ブラウザ側のブロックなど）はリロード・再入力しても解消しない恒久的な障害であるため、この挙動のまま変更しないことをチームで確認済み（2026-09-18）。

### 3-3. 体内残量（詳細）（`components/ResidualCard.tsx`）
- [ ] 大数値表示（現在の推定残存量、半減期5.0h表記、ピーク時刻・ピーク量表示）
- [ ] 減衰カーブ表示（上限ライン・現在位置・就寝時刻を含む）
- [ ] 就寝時の推定残存量、最後の1杯の時刻のサマリー表示
- [ ] 設定リスト（就寝時刻・半減期表示・1日の上限値の編集導線）

### 3-4. 履歴（`components/HistoryList.tsx`）
- [ ] 履歴一覧（直近1週間分を表示、日付ごとにグルーピング）
- [ ] 今日の飲み物別内訳表示
- [ ] 飲み物ごとの履歴フィルターUI
- [ ] 履歴削除機能（誤操作防止のための確認ステップ）
- [ ] フッター注記（「1週間より前の記録も端末内に保存されています」）

### 3-5. 統計（`app/stats/page.tsx` 等）
- [ ] 統計画面の骨組みを作成（表示内容の詳細仕様は別途検討し、決まり次第反映）

### 3-6. 設定（`app/settings/page.tsx`, `components/settings/SettingsScreen.tsx`）
- [x] 1日の摂取上限値の編集（フォーカスアウト・Enterで確定、無効値・保存失敗時はエラー表示のうえ直前の値を保持）
- [x] 就寝時刻の設定（`<input type="time">`で即時保存）
- [x] 半減期モデル値の表示（固定5時間、読み取り専用・変更不可の入力欄なし）

3-6 実装・検証メモ（2026-09-18）:

- デザインハンドオフでは設定画面は未設計（03画面の設定リストのみ）のため、そのリスト仕様（行padding16・区切り線）を流用してB案として新規実装。3人でのレビュー前提。
- `useSettings`をそのまま利用し、新しい状態管理は追加していない。上限値は下書きをローカルstateに保持しフォーカスアウト時にのみ`updateSettings`を呼ぶため、入力途中の不正値でグローバルなsettings状態を壊さない。就寝時刻はネイティブ`time`入力が常に完結した値のみ発火するため即時保存。
- ブラウザ検証（375px相当）: 上限値の編集・保存・再読み込み後の保持、0のような無効値でのエラー表示と直前値の保持、就寝時刻の変更・即時保存、設定読み込み失敗時のバナー表示と入力無効化を確認。
- `npx tsc --noEmit` / `npm run lint` 成功。ロジック層は変更していないため`npm test`は前回3-2実装時に確認済みの内容から変更なし。
- 未確認: A担当によるホーム統合後の他タブとの整合性、375px/402px/1280pxでの最終デザイン比較、C担当のResidualCard設定リストとの表示重複の整理。

## フェーズ4: ページ統合
- [ ] トップページに各コンポーネントを統合
- [ ] レスポンシブ対応（モバイル優先レイアウト）

## フェーズ5: PWA対応
- [ ] `manifest.json` の作成
- [ ] アプリアイコン画像の作成
- [ ] サービスワーカーの作成とアプリシェルのキャッシュ対応
- [ ] `layout.tsx` にmanifest・アイコン・テーマカラーのメタデータを設定

## フェーズ6: 動作確認・品質チェック
- [ ] TypeScript型チェック（`tsc --noEmit`）
- [ ] ESLint実行
- [ ] ブラウザでの動作確認
  - [ ] プリセット/手動での記録追加
  - [ ] 今日の総摂取量・残存量・内訳の更新確認
  - [ ] 残量カーブ表示、カットオフ・バナー（最後の1杯目安時刻）の表示確認
  - [ ] 履歴のフィルター・削除
  - [ ] 上限超過時の警告表示
  - [ ] 統計画面・設定画面（上限値／就寝時刻の変更）の動作確認
  - [ ] PWAとしてのインストール・オフライン起動

## フェーズ7: デプロイ
- [ ] GitHubリポジトリへプッシュ
- [ ] Vercelプロジェクトの作成・連携
- [ ] 本番環境での動作確認
